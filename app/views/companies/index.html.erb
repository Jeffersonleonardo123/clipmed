<div class="col-lg-12">
    <div class="col-lg-1">
    </div>
    <div class="col-lg-10">
        <p id="notice"><%= notice %></p>

        <h1>Empresas</h1>
        </br>
        </br>
        <div class="row">
            <%= link_to 'Nova Empresa', new_company_path,:class =>'btn btn-primary' %>
        </div>
        </br>
        </br>

        <table class="table table-striped">
          <thead>
            <tr>
            </tr>
          </thead>

          <tbody>
            <% @companies.each do |company| %>
              <tr>
                <td><%= company.name %></td>
                <td><%= number_with_delimiter(company.cnpj, delimiter: ".") %></td>
                <td><%= number_to_phone(company.fone) %></td>
                <td><%= company.email %></td>
                <td><%= link_to 'Editar', edit_company_path(company),{:class =>'btn btn-primary'}%></td>
                <td><%= link_to 'Excluir', company,:class =>'btn btn-danger', method: :delete, data: { confirm: 'Confirma a Exclusão?' }%></td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
    <div class="col-lg-1">
    </div>
<div>

<div class="col-lg-12">
    <div class="col-lg-2">
    </div>
    <div class="col-lg-8">

        <h1>Usuarios</h1>
        </br>
        </br>
        <div class="row">
            <%= link_to 'Novo', new_user_path,:class =>'btn btn-primary' %>
        </div>
        </br>
        </br>

        <table class="table table-striped">
          <thead>
            <tr>
            </tr>
          </thead>

          <tbody>
            <% @users.each do |user| %>
              <tr>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td><%= link_to 'Editar', edit_user_path(user),{:class =>'btn btn-primary'}%></td>
                <td><%= link_to 'Excluir', user,:class =>'btn btn-danger', method: :delete, data: { confirm: 'Confirma a Exclusão?' }%></td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
    <div class="col-lg-2">
    </div>
<div>
